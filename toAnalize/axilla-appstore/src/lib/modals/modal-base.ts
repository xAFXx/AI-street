import { Injector } from "@angular/core";

import { AppComponentBase } from "@axilla/axilla-shared";

export class ModalBase extends AppComponentBase {
    constructor(
        injector: Injector,
    ) {
        super(injector);
    }

    show() {

    }
}
