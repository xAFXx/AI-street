// Plattform development environment
// Uses appconfig-plattform.json which points to plattform.nl endpoints

export const environment = {
    production: false,
    hmr: false,
    appConfig: 'appconfig-plattform.json'
};
